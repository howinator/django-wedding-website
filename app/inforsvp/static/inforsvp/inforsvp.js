// todo only do this in dev
window.setTimeout(() => {
    const reload = false;
    if (reload){
        location.href = "http://localhost:7665";
    }
}, 5000);

$(() => {
    // do everything here
});
